# Skills 🧙‍♂️

Магический трекер навыков для волшебников Хогвартса. Приложение позволяет студентам отслеживать свой прогресс в изучении магических дисциплин, вести дневники практики и делиться своими достижениями.

🔗 **Демо**: [skills.aurorhq.ru](https://skills.aurorhq.ru)

## ✨ Особенности

- **Аутентификация Волшебников**: Вход и регистрация с проверкой имени (только кириллица) и "заклинания" (пароля).
- **Личный Кабинет**: Обзор всех навыков и текущего прогресса.
- **Трекинг Навыков**:
  - Беспалочковая магия
  - Невербальная магия
  - Трансгрессия
  - Анимагия
  - Мортимагия
- **Дневник Практики**: Возможность добавлять записи о тренировках (эссе минимум на 200 слов) с ссылками на доказательства.
- **Публичные Профили**: Возможность поделиться ссылкой на свой магический паспорт (например, `skills.aurorhq.ru/u/Гарри_Поттер`).
- **Атмосферный Дизайн**: Интерфейс выполнен в стиле пергаментов, с использованием шрифтов и цветов, вдохновленных вселенной Гарри Поттера.

## 🛠 Технологический стек

- **Frontend**: React, TypeScript, Vite
- **Styling**: Tailwind CSS (кастомная конфигурация цветов и шрифтов)
- **State Management**: Zustand
- **Backend / Database**: Supabase
- **Icons**: Lucide React

## 🚀 Установка и Запуск

### 1. Предварительные требования
- Node.js (версия 18 или выше)
- npm или yarn

### 2. Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/elena-yank/skills.git
   cd skills
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройка окружения**
   Создайте файл `.env` в корне проекта (можно скопировать `.env.example`, если он есть) и добавьте ключи вашего проекта Supabase:
   ```env
   VITE_SUPABASE_URL=https://your-project.supabase.co
   VITE_SUPABASE_ANON_KEY=your-anon-key
   ```

### 3. Запуск

**В режиме разработки:**
```bash
npm run dev
```
Откройте [http://localhost:5173](http://localhost:5173) в браузере.

## 📦 Сборка и Деплой

### Сборка проекта
Для создания оптимизированной версии для продакшена:
```bash
npm run build
```
Это создаст папку `dist` с готовыми статическими файлами.

### Деплой на сервер (например, Nginx)

Так как это SPA (Single Page Application), для корректной работы роутинга необходимо настроить перенаправление всех запросов на `index.html`.

Пример конфигурации Nginx для `skills.aurorhq.ru`:

```nginx
server {
    listen 80;
    server_name skills.aurorhq.ru;
    root /var/www/skills/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## � Использование собственной PostgreSQL (без Supabase)

Если вы хотите развернуть проект на собственном сервере с PostgreSQL:

1. **Подготовьте базу данных**
   Используйте файл `database/init.sql` для создания структуры таблиц в вашей PostgreSQL базе данных.
   ```bash
   psql -U postgres -d skills -f database/init.sql
   ```

2. **Запустите Backend Server**
   В папке `server/` находится Node.js приложение, которое служит API и отдает статику.
   
   ```bash
   cd server
   npm install
   ```
   
   Создайте `.env` в папке `server/`:
   ```env
   DATABASE_URL=postgres://user:password@localhost:5432/skills
   PORT=3000
   ```
   
   Запустите сервер:
   ```bash
   npm start
   ```

3. **Сборка Frontend для Custom API**
   При сборке укажите флаг использования REST API:
   
   ```bash
   # В корне проекта
   VITE_USE_REST_API=true VITE_API_URL=http://localhost:3000/api npm run build
   ```
   
   Скопируйте содержимое `dist/` в папку, которую обслуживает сервер (по умолчанию сервер ожидает `dist` в корне проекта).

## 🐳 Запуск в Docker

Самый простой способ запустить проект локально с собственной базой данных:

1. Убедитесь, что установлен [Docker](https://www.docker.com/) и Docker Compose.
2. Запустите команду:
   ```bash
   docker-compose up --build
   ```
3. Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000).

Docker Compose автоматически поднимет:
- Контейнер с PostgreSQL базой данных.
- Node.js сервер с уже собранным фронтендом.

## 📜 Структура Базы Данных (Supabase)

- **wizards**: Таблица пользователей (id, name, password).
- **practice_logs**: Журнал практики (id, user_id, skill_name, content, word_count, post_link, created_at).

## 🧙‍♂️ Разработка

Проект настроен для деплоя на `skills.aurorhq.ru`. 
В `vite.config.ts` установлен `base: '/'`.

---
*"Draco Dormiens Nunquam Titillandus"*
